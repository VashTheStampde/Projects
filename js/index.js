if(window.SimpleSlide){
    
    new SimpleSlide({
        slide: 'porti', // nome do atributo data-slide="principal"
        auto: true, // se o slide deve passar automaticamente
        time: 4000, // tempo de transição dos slides
      });
    
      new SimpleSlide({
        slide: 'portii', // nome do atributo data-slide="principal"
        auto: true, // se o slide deve passar automaticamente
        time: 4000, // tempo de transição dos slides
        nav: true
      });
}

